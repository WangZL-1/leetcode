class Solution {
    public int singleNumber(int[] nums) {
        int size=0;
        int res=0;
        while(size<nums.length){
            res^=nums[size];
            size++;
        }
        return res;
    }
}
